using System;
using System.Runtime.InteropServices;
namespace Basics
{
    class Program
    {
        public static void Main(string[] r)
        {
            //To be exectued
            // new StringManipulation().StringMan();
            // new Conditions().ConditionStatements();
            // new Loopings().Looping();
            // new Exceptions().ExceptionsEg();
            // new CollectionClasses().CollectionList();
            // new CollectionClasses().CollectionDictionary();
            // new ClassEg().MainMethod();
            // new MainClass().MainMethod();
            // new InheritanceEg().MainMethod();
            // new Poly().MainMethod();
            // new MyExtensions().MainMethod();
        }
    }
}